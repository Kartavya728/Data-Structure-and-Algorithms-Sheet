// If Else statements

#include <bits/stdc++.h>
using namespace std;

int main(){
    string s;
    cout<<"enter if or else to check it : ";
    cin>>s;
    if(s=="if")cout<<"if founded by the system ";
    else if(s=="else")cout<<"else founded by the system";
    else cout<<"no if or else is written by the user";
    return 0;
}